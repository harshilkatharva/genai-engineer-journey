[project]
name = "module-13-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.3.0",
    "mcp>=2.2.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
